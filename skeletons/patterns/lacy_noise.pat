{"type":-1,"data":"// noise mask\nlet mask = pat.filter(n => noise.simplex2(n.waleId/20, n.courseId/20) < 0);\n\n// tiled holes lace in noise mask\nmask.tiledo([\n'kkkk',\n'kllk',\n'kkkk',\n'krrk'\n])\n\n// adding knit margin + purl margins to emboss\nmask.neighbors(1:2).knit()\nmask.neighbors(3:4).purl()\n\n// enforce default knit on first 2 and last 2 courses\npat.courses(0:1).knit()\npat.courses(-1).knit()\npat.courses(-2).knit()","name":"Lacy Noise"}